#include "nygelA.h"
//#include "defs.h"
//#include "fonts.h"
extern Global gl;
void bar(){
/*    glColor3f(.5,.7,3);
    glBegin(GL_POLYGON);
    glVertex2f(gl.xres,	gl.yres);
    glVertex2f(0.0,	gl.yres);
    glVertex2f(0.0f,	25.0);
    glVertex2f(gl.xres,  0.0);
    glEnd();

    //Rect r;
    glClear(GL_COLOR_BUFFER_BIT);
    r.bot = gl.yres - 20;
    r.left = 10;
    r.center = 0;
    ggprint8b(&r, 16, 0x00ffff00, "Wave:%i \t\ttime:%s", 0 ,"1:30");

*/
    std::cout<<"hello";
}
